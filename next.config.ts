import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */

  images: {
    domains: ["https://orangetechconsultants.com"], // or any external host
  },
};

export default nextConfig;
